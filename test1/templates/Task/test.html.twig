
<div class="container login-container">
    <div class="row">
        <div class="col-md-6 login-form-2 form">
            <h3>Create New Account</h3>
            {{ form_start(taskForm, {'attr': {'class': 'form-group form'}}) }}

            <div class="form-group">
                {{ form_widget(taskForm.title) }}
            </div>
            <div class="form-group">
                <input type="submit" class="btnSubmit" value="Save" />
            </div>
            <div class="form-group">
                <a href="{{path('to_do_list')}}" class="ForgetPwd">CANCEL</a>
            </div>
            {{ form_end(taskForm) }}
        </div>
    </div>
</div>